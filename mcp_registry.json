{
  "servers": {
    "platform": {
      "transport": "stdio",
      "command": "uv",
      "args": ["run", "servers/platform_server.py"],
      "description": "Data platform — catalogue, ETL, dashboards, governance (15 tools)"
    },
    "gitlab": {
      "transport": "stdio",
      "command": "uv",
      "args": ["run", "servers/gitlab_server.py"],
      "description": "Mock GitLab MCP server — commits, diffs"
    }
  }
}
